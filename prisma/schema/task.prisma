model Alarm {
  uuid String @id @default(uuid())

  hour   Int
  minute Int

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Todo {
  uuid String @id @default(uuid())

  name     String
  remindAt DateTime?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
